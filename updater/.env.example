# Copy to .env locally, or inject via Infisical:
#   infisical run --env=dev -- python update_merge.py
#
# Used as OpenAlex/Crossref polite-pool User-Agent (not shown on the website).
OPENALEX_MAILTO=you@example.com

# Contact form: destination for onFormSubmit notifications (install_form_notify.py).
CONTACT_NOTIFY_EMAIL=you@example.com

# Reserved for future authenticated ResearchMap endpoints (currently unused).
# RESEARCHMAP_API_KEY=
